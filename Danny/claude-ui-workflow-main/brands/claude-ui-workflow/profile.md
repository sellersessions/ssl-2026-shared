# Claude UI Workflow — Meta brand

Not a real brand. This slug exists so the moodboard tool can hold internal
workflow diagrams (phase maps, tooling ASCII, planning artifacts) without
polluting real brand folders.

Canvas lives at `moodboard.tldr`. Open with:

    npm run moodboard -- claude-ui-workflow

Then browse to `http://localhost:5273/?brand=claude-ui-workflow`.
